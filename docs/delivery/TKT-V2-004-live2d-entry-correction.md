# TKT-V2-004 Live2D 入口修正与 Founder 验收清单

日期：2026-09-11  
状态：自动回归、真实浏览器检查和 Hiyori 冒烟通过；等待 Founder 人工复验

## 修正结论

- 此前 Founder 在浏览器里看到的是兼容表情形象，不是 Live2D。
- “受众反应类型”只负责选择受众模板，不再承载 Live2D 导入入口。
- “数字观众形象”现在是左侧训练设置中的独立常驻区，位于受众类型之后、折叠选题之前。
- 本机版显示可用的“选择本地模型文件夹”按钮。
- 网页版不再隐藏入口；它显示当前仍为兼容表情形象，并明确提示使用 GitHub 本机版。
- 网页版按钮保持禁用，因为网页包不携带仅获准用于本地开发验证的 Cubism Core 和官方模型。

## 自动与浏览器验证

- `npm test`：通过，退出码 0。
- `npm run package:web`：通过。
- 本机浏览器：入口、当前形象、导入按钮均在首屏可见；入口不在受众模板弹窗内。
- 网页打包版：常驻入口可见；按钮禁用；本机启动说明可见。
- Electron：`V2_LIVE2D_SMOKE_OK`，Hiyori 已导入、适配器 ready、画布存在、B 模式静音。

## Founder 人工验收清单

- [ ] 1. 切换到刚打开的 V2 本机窗口。预期：左栏“受众反应类型”下面直接出现独立的“数字观众形象”。失败时：截图左栏顶部到“压力等级”的完整区域。
- [ ] 2. 不打开“选择受众模板”弹窗，查看“数字观众形象”。预期：当前形象显示 `Hiyori · 官方本机验证样例`，而不是“兼容表情形象”。失败时：反馈当前形象框里的完整文字。
- [ ] 3. 查看中间观众窗口。预期：出现 Hiyori Live2D，不是白色圆脸。失败时：截图中间观众窗口，并反馈是否仍有白色脸。
- [ ] 4. 点击“选择本地模型文件夹”。预期：打开文件夹选择器，要求选择含一个 `.model3.json` 的模型文件夹；取消后原模型不变。失败时：反馈按钮是否可点及页面提示文字。
- [ ] 5. 点击“预览反应”，再开始一段连续口播。预期：Hiyori/兼容形象只改变表情，应用不发声、不追问、不打断。失败时：反馈听到的声音内容和发生时间。

不要用原来的公开网址验收本轮 Hiyori：本轮只修改了本地工作区并生成网页包，尚未提交、推送或部署。公开网址在部署前仍可能是旧版本。

任何一项失败，即本轮不通过；自动测试结果不能覆盖人工验收结论。
