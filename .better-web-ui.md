## Design Context

### Users

自媒体新手及创作者，用镜头练习减少口头禅、废话和模糊表达。V1/V2/V3 是同一诊断底座上的训练层，不扩展为通用面试、外语考试或会议软件。产品同时提供浏览器和桌面运行方式；浏览器的权限或转写失败必须清楚说明，不能伪装成用户操作错误。

### Brand Personality

Read Yourself：有创始人个性、专业、克制。保留用户自己编辑的标题、文案与配色；不替用户敲定新的品牌表达。训练工具应该鼓励但不甜腻，诚实说明什么已经工作、什么尚未可用。

### Aesthetic Direction

用户已批准 Magic UI Vertical Marquee 用于首页右上角。该区域只有连续的句子，无卡片底色、渐变、圆角、边框、标签、状态灯或模糊。普通句用问题词下划线或 highlight；优化句通过字重、字号与颜色建立层级。只采用免费、许可清晰的组件。保留现有的 Radix 配色、眼睛相机 Logo 和动态文字；训练流程比首页更安静，避免通用仪表盘卡片、嘈杂渐变和装饰性错误状态。

### Implementation Defaults

沿用现有 Electron + 原生 HTML/CSS/JavaScript 与 CSS Token；不为单个 React 组件迁移整站。组件参数在独立调控板页管理，接入现有 localStorage 草稿和 creator-project-config.js 项目保存。网页由 Cloudflare Pages 打包，桌面端使用 Sherpa-ONNX；浏览器能力必须特性检测和渐进增强，不根据浏览器名称硬编码。任何变更优先遵循现有栈，再考虑用户明确选择，不套用新项目默认框架。

### Design Principles

1. 排版先于标签：用户应直接看懂表达改善，不靠“原句/精炼”说明。
2. 透明就是没有额外表面；渐隐只能作用于文字透明度。
3. 连续滚动只动画 transform；可暂停，尊重减少动态效果。
4. 调控入口按组件命名，切换可往返，原有草稿和用户文案不得丢失。
5. 演示不假装实时 AI：精简示例保留原意，不编造数据、经历或承诺；权限、录制和转写状态必须诚实可行动，识别服务失败不能说成麦克风失败。
