## Design Context

### Users
自媒体新手及创作者，用镜头练习减少口头禅、废话和模糊表达。V1/V2/V3 是同一诊断底座上的训练层，不扩展为通用面试、外语考试或会议软件。

### Brand Personality
Read Yourself：有创始人个性、专业、克制。保留用户自己编辑的标题、文案与配色；不替用户敲定新的品牌表达。

### Aesthetic Direction
用户已批准 Magic UI Vertical Marquee 用于首页右上角。该区域只有连续的句子，无卡片底色、渐变、圆角、边框、标签、状态灯或模糊。普通句用问题词下划线或 highlight；优化句通过字重、字号与颜色建立层级。只采用免费、许可清晰的组件。

### Implementation Defaults
沿用现有 Electron + 原生 HTML/CSS/JavaScript 与 CSS Token；不为单个 React 组件迁移整站。组件参数在独立调控板页管理，接入现有 localStorage 草稿和 creator-project-config.js 项目保存。任何变更优先遵循现有栈，再考虑用户明确选择，不套用新项目默认框架。

### Design Principles
1. 排版先于标签：用户应直接看懂表达改善，不靠“原句/精炼”说明。
2. 透明就是没有额外表面；渐隐只能作用于文字透明度。
3. 连续滚动只动画 transform；可暂停，尊重减少动态效果。
4. 调控入口按组件命名，切换可往返，原有草稿和用户文案不得丢失。
5. 演示不假装实时 AI：精简示例保留原意，不编造数据、经历或承诺。
